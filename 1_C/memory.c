#include <stdio.h>
#include <string.h>

/*
** void * memset ( void * ptr, int value, size_t num );
** 通常这个函数可以将一大段内存设置为某一个具体数值。非常快，经过系统优化。不用循环
** void * memcpy ( void * destination, const void * source, size_t num );
** 使用num段长度的source拷贝到destination.也是系统级别的，很快。
*/

